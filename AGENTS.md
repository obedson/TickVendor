# TickVendor - Agent Entry Point

Read and follow `docs/AGENTS.md` before changing anything in this repository. Those are the permanent operating rules for every agent session.

* `docs/TICKVENDOR_SPEC.md` is the authoritative product specification and completion contract.
* For recurring engineering work, select only the relevant workflow from `docs/agent-workflows/README.md`.
* Do not automatically load every workflow, the historical reconciliation documents, or the full traceability matrix for a bounded task.
* Preserve the current working tree, including intentional tracked and untracked work, and follow the push, deploy, and external-action restrictions in `docs/AGENTS.md`.
