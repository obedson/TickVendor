# TickVendor app icons

User-requested branding extension: use the approved ticket/checkmark logo as the
installed app icon. Generated a text-free square mark using the built-in image
generation tool (imagegen skill), then mechanically resized using System.Drawing.

Final prompt: Create the square installed-app icon variant of this TickVendor logo.
Preserve its golden ticket with side notches and dark green rounded square with
golden checkmark. Remove all wordmark and tagline text. Flat crisp graphic, no
shadows or gradients. Solid opaque forest green #123F35 background filling the
entire square edge to edge, no outer rounded corners. Center the ticket/checkmark
symbol, keep the entire symbol within the central 70 percent of the canvas in both
dimensions for safe circular mobile OS masking. Single square icon, no mockup,
no lettering, no border. 1024x1024.

User correction: the generated variant was rejected. The final source is the exact
user-supplied `codex-clipboard-93278eed-90a4-4bc5-98d8-4c80c8a1c150.png`, copied without
artistic editing. Resizing preserves aspect ratio and samples its own background
for square padding; the mark fits within 70% width for safe masking. The generation
prompt above records the discarded variant only, not the final artwork.

Source: `frontend/public/tickvendor-icon-source.png`. Outputs:
`tickvendor-icon-v1-{32,180,192,512}.png` in the same folder. Regenerate on Windows
with `node frontend/scripts/generate-icons.mjs`. Builds use checked-in images and
do not require Windows or image libraries. Old icons remain for older clients.

Manifest uses 192/512 icons, with maskable purpose on 512; HTML links favicon and
Apple touch icon. Versioned filenames and service-worker cache v6 avoid old icon
responses. Start URL/scope remain unchanged to preserve installation identity.

Visual inspection: 192px output retains a clear ticket/checkmark and safe padding.
Actual Android/iOS installation and existing-install icon refresh remain unverified.
No commit, push or deployment performed. Existing installations may require an OS
refresh or reinstall after deployment; do not promise immediate replacement.

Initial PWA test exposed an outdated assertion looking for the install label in
main.tsx. Label lives in AppShell.tsx; assertion now checks that actual component.

## Publication CI follow-up

PR #13 exposed six pre-existing migration-test failures (417 passes). The user
approved repairing the blockers. Four assertions expected the former head rather
than `0a1b2c3d4e5f`. Two legacy wallet fixtures inserted today's Event ORM into the
pre-accuracy schema. Fixture now uses reflected historical columns and explicit
event values; normalization, preservation and current wallet API checks remain.
All nine tests in the three affected files passed locally, as did changed-file
Ruff and whitespace checks. No migration or production behavior was modified.
