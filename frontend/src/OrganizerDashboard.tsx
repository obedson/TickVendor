import{useEffect,useState}from'react';
type Summary={upcoming_events:number;total_events:number;ticket_sales:number;revenue:string;registrations:number;attendance:number;verified_attendance:number;pending_tasks:number;contributions:number;engagement:number};
export function OrganizerDashboard({token}:{token:string}){const[data,setData]=useState<Summary|null>(null);const[error,setError]=useState('');useEffect(()=>{fetch('/api/v1/communities/organizer/dashboard',{headers:{Authorization:`Bearer ${token}`}}).then(async r=>{if(!r.ok)throw Error(r.status===403?'Organizer access required':'Unable to load dashboard');return r.json()}).then(setData).catch(e=>setError(e.message))},[token]);if(error)return <section className="panel"><h2>Organizer dashboard</h2><p role="alert" className="error">{error}</p></section>;if(!data)return <section className="panel"><h2>Organizer dashboard</h2><p role="status">Loading dashboard…</p></section>;return <section aria-labelledby="organizer-dashboard-title"><p className="eyebrow">Operations</p><h2 id="organizer-dashboard-title">Organizer dashboard</h2><div className="grid">{[['Upcoming events',data.upcoming_events],['Total events',data.total_events],['Ticket sales',data.ticket_sales],['Registrations',data.registrations],['Attendance',data.attendance],['Verified attendance',data.verified_attendance],['Pending tasks',data.pending_tasks],['Contributions',data.contributions],['Engagement points',data.engagement],['Revenue',data.revenue]].map(([label,value])=><article className="panel card" key={String(label)}><h3>{label}</h3><p className="metric">{value}</p></article>)}</div></section>}
