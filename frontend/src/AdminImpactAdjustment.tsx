import { useEffect, useState } from 'react';
import { apiFetch } from './api';

type Member = { user_id: string; display_name?: string; username?: string; status: string };
export function AdminImpactAdjustment({ token, communityId }: { token: string; communityId?: string }) { const [community, setCommunity] = useState(communityId); const [members, setMembers] = useState<Member[]>([]); const [target, setTarget] = useState(''); const [amount, setAmount] = useState(''); const [reason, setReason] = useState(''); const [error, setError] = useState(''); const [message, setMessage] = useState(''); const [busy, setBusy] = useState(false); const headers = { Authorization: `Bearer ${token}` }; const load = async (id = community) => { if (!id) return; const response = await apiFetch(`communities/${id}/members`, { headers }); if (!response.ok) { setError('Unable to load community members.'); return; } setMembers((await response.json()).members); }; useEffect(() => { if (community) load(); else apiFetch('communities/me', { headers }).then(response => response.json()).then(items => { const id = items[0]?.id; setCommunity(id); if (id) load(id); }).catch(() => setError('Unable to load communities.')); }, [token, community]); const submit = async (event: React.FormEvent) => { event.preventDefault(); if (!community || !target || Number(amount) === 0) return; setBusy(true); setError(''); setMessage(''); const response = await apiFetch(`admin/communities/${community}/point-adjustments`, { method: 'POST', headers: { ...headers, 'Content-Type': 'application/json' }, body: JSON.stringify({ target_user_id: target, amount: Number(amount), reason }) }); const data = await response.json(); if (!response.ok) setError(data.detail || 'Unable to adjust Impact Points.'); else setMessage(`Impact Points adjusted by ${data.points}. Reference ${data.id}.`); setBusy(false); }; return <section className="panel"><h2>Manual Impact Point adjustment</h2><p>Adjustments are append-only and audited.</p>{error && <p role="alert" className="error">{error}</p>}{message && <p role="status">{message}</p>}<form onSubmit={submit}><label>Participant<select required value={target} onChange={e => setTarget(e.target.value)}><option value="">Select a member</option>{members.filter(member => member.status === 'active').map(member => <option key={member.user_id} value={member.user_id}>{member.display_name || member.username || 'Private member'}</option>)}</select></label><label>Amount<input required type="number" min="-100000" max="100000" value={amount} onChange={e => setAmount(e.target.value)} /></label><label>Reason<textarea required minLength={3} value={reason} onChange={e => setReason(e.target.value)} /></label><button disabled={busy || !target || Number(amount) === 0}>{busy ? 'Submitting…' : 'Adjust points'}</button></form></section>; }
