import { useEffect, useState } from 'react';

type Board = { id: string; name: string; slug: string; metric: string; period: string; max_entries: number; is_enabled: boolean; event_id?: string | null };
export function AdminLeaderboards({ token, communityId }: { token: string; communityId?: string }) { const [community, setCommunity] = useState(communityId); const [boards, setBoards] = useState<Board[]>([]); const [form, setForm] = useState({ name: 'Community leaderboard', slug: 'community-leaderboard', metric: 'overall', period: 'all_time', max_entries: '100', is_enabled: 'true' }); const [error, setError] = useState(''); const [message, setMessage] = useState(''); const headers = { Authorization: `Bearer ${token}` }; const load = async (id = community) => { if (!id) return; const response = await fetch(`/api/v1/admin/communities/${id}/leaderboards`, { headers }); if (!response.ok) { setError(response.status === 403 ? 'Administrator access required.' : 'Unable to load leaderboard configuration.'); return; } setBoards(await response.json()); }; useEffect(() => { if (community) load(); else fetch('/api/v1/communities/me', { headers }).then(response => response.json()).then(items => { const id = items[0]?.id; setCommunity(id); if (id) load(id); }).catch(() => setError('Unable to load communities.')); }, [token, community]); const save = async (event: React.FormEvent) => { event.preventDefault(); if (!community) return; const existing = boards[0]; const response = await fetch(`/api/v1/admin/communities/${community}/leaderboards${existing ? `/${existing.id}` : ''}`, { method: existing ? 'PATCH' : 'POST', headers: { ...headers, 'Content-Type': 'application/json' }, body: JSON.stringify({ ...form, max_entries: Number(form.max_entries), is_enabled: form.is_enabled === 'true' }) }); const data = await response.json(); if (!response.ok) { setError(data.detail || 'Unable to save leaderboard.'); return; } setMessage('Leaderboard configuration saved.'); await load(); }; return <section className="panel"><h2>Leaderboard configuration</h2>{error && <p role="alert" className="error">{error}</p>}{message && <p role="status">{message}</p>}<form onSubmit={save}><label>Name<input required value={form.name} onChange={e => setForm({ ...form, name: e.target.value })} /></label><label>Slug<input required value={form.slug} onChange={e => setForm({ ...form, slug: e.target.value })} /></label><label>Metric<select value={form.metric} onChange={e => setForm({ ...form, metric: e.target.value })}><option value="overall">Overall</option><option value="attendance">Attendance</option><option value="tasks">Tasks</option><option value="service">Service</option><option value="leadership">Leadership</option><option value="event">Event</option></select></label><label>Period<select value={form.period} onChange={e => setForm({ ...form, period: e.target.value })}><option value="all_time">All time</option><option value="weekly">Weekly</option><option value="monthly">Monthly</option></select></label><label>Maximum entries<input required type="number" min="1" max="500" value={form.max_entries} onChange={e => setForm({ ...form, max_entries: e.target.value })} /></label><button>Save leaderboard</button></form>{!error && !boards.length && <p className="empty">No leaderboard configured.</p>}{boards.map(board => <article className="card" key={board.id}><strong>{board.name}</strong><p>{board.metric} · {board.period} · {board.max_entries} entries · {board.is_enabled ? 'Enabled' : 'Disabled'}</p></article>)}</section>; }
