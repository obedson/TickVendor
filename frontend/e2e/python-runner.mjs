import{spawn}from'node:child_process';import{existsSync}from'node:fs';import{resolve}from'node:path';
const root=resolve(import.meta.dirname,'../..');const candidates=process.platform==='win32'?['venv/Scripts/python.exe','.venv/Scripts/python.exe']:['venv/bin/python','.venv/bin/python'];const python=candidates.map(path=>resolve(root,path)).find(existsSync)||'python';const args=process.argv.slice(2).map((arg,index)=>index===0&&arg.startsWith('e2e/')?`frontend/${arg}`:arg);const child=spawn(python,args,{cwd:root,stdio:'inherit'});const forward=signal=>child.kill(signal);process.on('SIGTERM',()=>forward('SIGTERM'));process.on('SIGINT',()=>forward('SIGINT'));child.on('exit',(code,signal)=>process.exit(code??(signal?1:0)));
