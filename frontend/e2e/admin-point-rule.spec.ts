import { test, expect } from '@playwright/test';

test('administrator can persist a point rule mutation', async ({ page }) => { await page.goto('/'); await page.getByLabel('Email').fill('e2e-admin@example.com'); await page.getByLabel('Password').fill('e2e-password-123'); await page.getByRole('button', { name: 'Sign in' }).click(); await page.getByRole('button', { name: 'Point rules' }).click(); await expect(page.getByRole('heading', { name: 'Point rules' })).toBeVisible(); await page.getByLabel('Source type').fill('admin_acceptance'); await page.getByLabel('Points').fill('17'); await page.getByRole('button', { name: 'Save point rule' }).click(); await expect(page.getByRole('status')).toContainText(/saved/i); await expect(page.locator('body')).toContainText('admin_acceptance'); });
